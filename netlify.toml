# Static site — no build step. Netlify serves the public/ folder as-is.

[build]
  publish = "public"

# Pretty URL for the pitch deck: /deck -> /deck.html
[[redirects]]
  from = "/deck"
  to = "/deck.html"
  status = 200
